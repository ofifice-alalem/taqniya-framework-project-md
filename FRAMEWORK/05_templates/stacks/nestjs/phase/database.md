# NestJS Phase [PHASE_NUMBER] Database Deliverables

# 1. Models & Relations
- Tables and relationships for this phase in `prisma/schema.prisma`.
- Migration scripts generated via `npx prisma migrate dev`.

---

## 2. Seed Data
- Phase development seeders in `prisma/seed.ts`.
