# Laravel Phase [PHASE_NUMBER] Pest Testing Plan

# 1. Test Suites
- Unit: `tests/Unit/*`
- Feature: `tests/Feature/*`

# 2. Command
```bash
php artisan test --filter=[PHASE_OR_FEATURE]
vendor/bin/phpstan analyse
vendor/bin/pint --test
```
